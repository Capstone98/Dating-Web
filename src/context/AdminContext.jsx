import { createContext } from 'react';

// Create contexts for state, actions, and functions
export const AdminStateContext = createContext();
export const AdminActionsContext = createContext();
export const AdminFunctionsContext = createContext();
